import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log(await prisma.user.findMany({ select: { email: true, isActive: true } })); } main();
