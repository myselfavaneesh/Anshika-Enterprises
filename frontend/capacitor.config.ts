import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anshika.enterprises',
  appName: 'Anshika Enterprises',
  webDir: 'dist',
  server: {
    cleartext: true
  }
};

export default config;
